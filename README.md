# Alpha(α) 

This is walkthrough "Writing Interpreter in Go".

```
type Student {
	var name string
	var phone string

	func ContactInfo() string {
		return name + phone
	}
}

var s Student
s.name = "John"
s.phone = "0123456789"
```
